
__pycache__/
*.pyc

# Source workbooks are never committed. They are large binaries that grow a new
# copy in git history on every re-save, and they carry more internal detail than
# the site needs. Only the generated schmootz_data.json is committed.
/Schmootz.xlsx
/data/Schmootz.xlsx
/data/*.xlsx

# Backtest outputs regenerate identically from the scripts in analysis/. Only
# the markdown reports are committed.
analysis/*.csv
